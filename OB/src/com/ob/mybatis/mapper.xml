<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- mapper 태그의 namespace 속성 필수 작성 -->
<mapper namespace="Room">

<!-- ******공지사항 관련****************************************** -->
	<select id="noticeTotalCount" resultType="int">
		SELECT COUNT(ID) FROM OBHOTEL_BOARD
	</select>
	
	<select id="noticeList" resultType="com.ob.vo.BoardplusVO" parameterType="map">
		SELECT *
		  FROM (SELECT ROWNUM R_NUM, B.*
          FROM (SELECT *
                  FROM OBHOTEL_BOARD
                 WHERE BOARD_TYPE = #{board_type} 
                 ORDER BY ID DESC
               ) B
		  )
		WHERE R_NUM BETWEEN #{begin} AND #{end}
	</select>
	
	<insert id="noticeInsert" parameterType="com.ob.vo.BoardplusVO">
		INSERT INTO OBHOTEL_BOARD
			(ID, USER_ID, BOARD_TYPE, TITLE, CONTENT, WRITE_DATE, READ_COUNT)
		VALUES (OBHOTEL_BOARD_SEQ.NEXTVAL, #{user_id}, #{board_type}, #{title}, #{content}, SYSDATE, 0)
	</insert>
	
	<!-- 조회수 수정(1증가) -->
	<update id="noticeHit" parameterType="com.ob.vo.BoardplusVO">
		UPDATE OBHOTEL_BOARD SET READ_COUNT = #{read_count} + 1 WHERE ID = #{id}
	</update>
	
<!-- ******idcheck************************************* -->
	<select id="checkId" resultType="com.ob.vo.UserVO"
		parameterType="String">
			SELECT *
			FROM OBHOTEL_USER
			WHERE ACCOUNT = #{account}
	</select>
	
	<insert id="checkJoin" parameterType="com.ob.vo.UserVO">
		INSERT INTO 테이블명 SET NAME=#{name}, 
	</insert>

</mapper>