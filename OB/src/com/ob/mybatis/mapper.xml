<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- mapper 태그의 namespace 속성 필수 작성 -->
<mapper namespace="Room">
	
	
<!-- ****** 게시판 공동 ************************************************* -->
	<!-- 게시판별 전체 게시물 조회 -->
	<select id="getList" resultType="com.ob.vo.BoardplusVO" parameterType="String">
		SELECT * FROM OBHOTEL_BOARD WHERE BOARD_TYPE = #{board_type} ORDER BY ID DESC
	</select>
		
	<!-- 게시판별 총 게시물 건수 조회 -->
	<select id="totalCount" resultType="int" parameterType="String">
		SELECT COUNT(*) FROM OBHOTEL_BOARD WHERE BOARD_TYPE = #{board_type}
	</select>

	
	<!-- 프리미엄후기 총 건수 조회 -->
	<select id="totalPremiumCount" resultType="int">
		SELECT COUNT(*) FROM OBHOTEL_BOARD
	</select> 
	
	<!-- 프리미엄후기 페이지별 목록조회  -->
<!-- 	<select id="getPremPageList" resultType="com.ob.vo.PremiumVO"
	
	<select id="getPremPageList" resultType="com.ob.vo.PremiumVO"
			parameterType="Map">
		SELECT 	*
		  FROM (SELECT ROWNUM R_NUM, B.*
		  		  FROM (SELECT * FROM OBHOTEL_BOARD ORDER BY ID DESC) B)
=======
		-->
	<!-- 게시판 페이지별 목록 조회 -->
	<select id="getPageList" resultType="com.ob.vo.BoardplusVO"
		parameterType="Map">
		SELECT *
		  FROM (SELECT ROWNUM R_NUM, B_LIST.*
		  		  FROM (SELECT *
		  		  		  FROM OBHOTEL_BOARD
		  		  		 WHERE BOARD_TYPE = #{board_type} ORDER BY ID DESC) B_LIST)
>>>>>>> refs/remotes/origin/master
		 WHERE R_NUM BETWEEN #{begin} AND #{end}
<<<<<<< HEAD
		   AND TYPE=#{bbs_type}
	</select> -->
=======
	</select>

>>>>>>> refs/remotes/origin/master
<!-- ***************************************************************** -->


<!-- ****** 로그인 아이디체크 ******************************************** -->
	<!-- 아이디 체크 -->
	<select id="checkId" resultType="com.ob.vo.UserVO"
		parameterType="String">
			SELECT *
			FROM OBHOTEL_USER
			WHERE ACCOUNT = #{account}
	</select>
	
	<!-- 회원가입 체크 -->
	<insert id="checkJoin" parameterType="com.ob.vo.UserVO">
		INSERT INTO 테이블명 SET NAME=#{name}, 
	</insert>
	
<!-- ****** 공지사항 ************************************************** -->

	<select id="noticeTotalCount" resultType="int">
		SELECT COUNT(ID) FROM OBHOTEL_BOARD
	</select>
	
	<select id="noticeList" resultType="com.ob.vo.BoardplusVO" parameterType="map">
		SELECT R_NUM, ID, USER_ID, BOARD_TYPE, CONTENT, 
			   WRITE_DATE, READ_COUNT
		  FROM (SELECT ROWNUM R_NUM, B.*
		          FROM (SELECT *
		                  FROM OBHOTEL_BOARD
		                 WHERE BOARD_TYPE = #{board_type} 
		                 ORDER BY ID DESC
		               ) B
		        )
		WHERE R_NUM BETWEEN #{begin} AND #{end}
	</select>
	
<!-- ******idcheck************************************* -->
	<select id="checkId" resultType="com.ob.vo.UserVO"
		parameterType="String">
			SELECT *
			FROM OBHOTEL_USER
			WHERE ACCOUNT = #{account}
	</select>
	
	<!--****checkJoin********************************** -->
	<insert id="checkJoin" parameterType="com.ob.vo.UserVO">
		INSERT INTO OBHOTEL_USER 
			(ID, ACCOUNT, PASSWORD, NICKNAME, BIRTHDAY,ZIPCODE , ADDRESS1, ADDRESS2, EMAIL,E_CONFIRM,TEL)
		VALUES (OBHOTEL_USER_SEQ.NEXTVAL, #{account}, #{password},
			#{nickname}, #{birthday},#{zipcode} ,#{address1}, #{address2}, #{email},#{e_confirm}, #{tel})
	</insert>

</mapper>
